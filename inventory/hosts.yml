---
all:
  children:
    servers:
      hosts:
        remote-server:
          ansible_host: 1.2.3.4
          ansible_user: test
          ansible_ssh_private_key_file: /home/control/.ssh/devops_vm
